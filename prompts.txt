Prompt 1:
closing_worksheet.txt is a paper worksheet used to manually calculate the income and expenses for a dance event. The person enters the income amounts and the contract rates for each of the artists, and then manually calculates the income, expenses, and bonus amounts.
Create a new javascript app that allows the user to enter the cash amounts, electronic payment amount, and the contract rates for the artists. the program should automatically calculate the remaining amounts, updating them immediately as numbers are entered.
The app should be as simple as possible, following the general layout of closing_worksheet.txt . do not use any external libraries, only built-in javascript features.

Prompt 2:
move each of the existing elements into one of 3 columns.
column 1 contains labels and descriptions.
column 2 contains the input fields.
column 3 contains the calculated values.
Also, save all of these prompts into a file called prompts.txt .

Prompt 3:
change the rent from a predefined value into an input field. prefill it with 250.
prefill the caller, sound, band1, and band2 fields with 75.

Prompt 4:
the label next to Bonus should be changed to say "(10% if positive, rounded up to nearest $5)".
When calculating the bonus, round up to nearest $5.
Also, please fix the border-top lines, which are not always lining up across the three columns. same with the border-bottom lines.
Also, automatically use the system's light or dark mode for the page.

Prompt 5:
Some of the calculated values such as "Total Cash" have a light background even in dark mode, which makes it hard to read. please adjust to be readable in dark or light mode.
When a row on the page has no input field in the second column, allow the labels in the first column to expand into the second column.
remove the "Summary" line.

Prompt 6:
make the layout responsive so that the app can be used on a phone.

Prompt 7:
Remove all coloring and highlighting from calculated fields.
When a user clicks or taps into an input field, the contents of that field should be selected.
Add "(under development)" after "Event Closing Worksheet" in both the title and the header.
body max-width should be 400px.
col-label class should be text-align: right.

Prompt 8:
Change title to "Closing Worksheet".
Change header to "Closing Worksheet v0.1".
Change the label for the bonus to "Bonus (10%, rounded up, nearest $1)". Change the calculation to round up to the nearest $1 (instead of $5).
Keep labels for input fields right justified, but change all other labels to be left justified.

Prompt 9:
Move the input fields for electronic payments, caller, sound, band members, and rent to the right column.

Prompt 10:
add a new row at the end of the page. this row should have 3 divs, a div with a col-label class, a div with a col-input class, and a div with a col-result class. The first div should have the text "Bonus - Actual:". The second div should have an input button that says "Copy". The third div should hold a number with a default of $0.00.
When the copy button is pressed, the value from the result on the previous row should be copied to the result on this row.

Prompt 11:
Change the label on the next-to-last row to say "Bonus - Calculated".
Make the label on the last row left justified.
Change the label on the copy button to say "Copy calculated".
Make the result in the last column an input box so that a value can be manually input. The copy button should still copy the value from the previous row when pressed.

Prompt 12:
change the seven rows starting with "caller" and ending with "band member 5" so that they match the layout of the rows following "Cash count".  There should be a div with a col-label class, a div with a col-input class, and a div with a col-result class in that order for each row.
The input field in col-result div should be moved to col-input div.
The  col-result div should be the result of the col-input div input added to the the bonusActual input. This value should be automatically updated whenever either of those two values change.

Prompt 13:
for the 7 artist rows, if col-input is equal to 0, col-result should remain 0.00.

Prompt 14:
Change the labels to replace "Band member" with just "Band", keeping the existing number in each label.
For the 7 artist rows, add a new column between columns 2 and 3. It should be another result column with a default of $0.00 . Whenever the bonusActual value is changed, that value should be copied to this new column in all 7 artist rows.
Change "Artist Contract Rates" to "Artist Rates".
Add a new row before the "Caller" row. The first column should be blank. The next 3 columns should contain the words "Contract", "Bonus" and "Total", respectively.

Prompt 15:
For the bonus column in the artist rows, add " + " before the number and add a little horizontal space at the left edge of that column.
Add a little vertical space between the "Total Artists" row and the one that follows it because they're almost overlapping right now.
Change the page header to say "v0.2" instead of "v0.1".

Prompt 16:
The "Total Artists" and "Rent" rows should have 4 columns that line up with the artist columns above them. The result value for "Total Artists" should be in the second column instead of the last column. The input for Rent should also be in the second column instead of the last column. The label "Expenses (Artists + Rent)" should be changed to "Pre-bonus Expenses (Artists + Rent)". Change the label "Net Income (Income - Expenses)" to "Pre-bonus Net Income (Income - Expenses)". Change "Total Artists" to "Artist Totals".

Prompt 17:
Change "Artist Rates" to "Expenses (Artists + Rent)". Remove the "Artist Totals" row. The Rent row should become part of the artist grid right under the "Band 5" row with rent label in column 1, rent input in column 2, and columns 3-4 empty. The "Pre-bonus Expenses" row should become part of the artist grid with "Expenses:" in column 1, the expenses span in column 2, and columns 3-4 blank. The "Pre-bonus Net Income" row should become part of the artist grid with "Net Income:" in column 1, the net income span in column 2, and columns 3-4 blank.

Additional prompts (with a blank line between each):
The rows for Rent, Expenses, Net Income, should use the exact same classes for each of the columns as the rows in the artist grid. Right now the columns for these rows are not lining up with the rows in the artist grid.

Change the label from "Expenses" to "pre-bonus Expenses" and "Net Income" to "pre-b. Net Income".

Add a text paragraph before "Cash Count" that says "Put $40 in $5 bills and $10 in singles in metal box; do not count as Cash. Then count remaining Cash:".
Under Count Cash, before the "$1" label add the text "(or enter entire amount here)".
Increase body width to 440px.
Change the label "pre-b. Net Income" to "Pre-bonus Net Inc".
Change the label "pre-bonus Expenses" to "Pre-bonus Expense".
Add a new column at the end similar to the "Income" line, but with the label "Deposit (Cash - Rent - Artist Total)". The result column should always equal totalCash - rent - callerTotal - soundTotal - band1Total - band2Total - band3Total - band4Total - band5Total.

Remove the div containing "(or enter entire amount here)".
Change the label "$1 x" to "(or enter amount here) $1 x".
Make the Deposit row the last row on the page.
Add a line break before "Then count remaining Cash:".
Change version to "v0.3".

Make "Electronic Payments:" left-justified. Try to re-use existing classes, if possible.

Change both the label and the formula for the Deposit to be Income - Rent - Artist Total.

After the page has loaded, check the query string to see of there are any keys that match the names of input fields in this page. If such values are found and they are numeric and >= 0 and <= 2000, use the value of each key to populate the value of the corresponding input, overwriting any default value. Ignore any keys that don't match the name of any input field.
Any time an input value is changed, create or update the query string to include that input's name and value.

Change both the label and the formula for the Deposit to be Total Cash - Artist Total.

Change the label for "Electronic Payments" to be "Electronic Payments (Zeffy)".

Change the label for "Electronic Payments (Zeffy)" to "Zeffy/Electronic Payments".

Change the label for "Income (Cash + Electronic)" to "Total (Cash + Zeffy/Electronic)".
