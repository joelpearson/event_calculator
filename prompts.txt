Prompt 1:
closing_worksheet.txt is a paper worksheet used to manually calculate the income and expenses for a dance event. The person enters the income amounts and the contract rates for each of the artists, and then manually calculates the income, expenses, and bonus amounts.
Create a new javascript app that allows the user to enter the cash amounts, electronic payment amount, and the contract rates for the artists. the program should automatically calculate the remaining amounts, updating them immediately as numbers are entered.
The app should be as simple as possible, following the general layout of closing_worksheet.txt . do not use any external libraries, only built-in javascript features.

Prompt 2:
move each of the existing elements into one of 3 columns.
column 1 contains labels and descriptions.
column 2 contains the input fields.
column 3 contains the calculated values.
Also, save all of these prompts into a file called prompts.txt .
